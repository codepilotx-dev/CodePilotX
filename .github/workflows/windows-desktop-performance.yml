name: Windows desktop performance

on:
  schedule:
    - cron: "0 18 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: windows-desktop-performance
  cancel-in-progress: true

jobs:
  renderer-observation:
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.14
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Renderer performance observation
        run: bun scripts/performance/run-suite.ts --suite=renderer
      - name: Upload renderer performance report
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: renderer-observation-${{ github.run_attempt }}
          path: performance-results/renderer/
          if-no-files-found: warn
          retention-days: 30

  packaged-electron:
    runs-on: windows-latest
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.14
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Build Windows package
        run: bun run package:win
      - name: Locate packaged executable
        shell: pwsh
        run: |
          $executable = Get-ChildItem release -Recurse -Filter CodePilotX.exe |
            Where-Object { $_.FullName -match 'win-unpacked' } |
            Select-Object -First 1
          if (-not $executable) {
            throw "CodePilotX.exe was not found in the unpacked package"
          }
          "CODEPILOTX_PERF_EXECUTABLE=$($executable.FullName)" |
            Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
      - name: Packaged Electron performance
        run: bun run perf:electron
      - name: Upload performance report
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: windows-desktop-performance
          path: performance-results/electron/
          if-no-files-found: warn
          retention-days: 30
