/**
 * OAuth client for authentication flows with Claude services.
 *
 * Re-exported from @codepilotx/core so both packages share the same
 * implementation.
 */
export * from '@codepilotx/core/services/oauth/client.js'
