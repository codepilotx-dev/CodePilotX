/**
 * OAuth constants and configuration.
 *
 * Re-exported from @codepilotx/core so both packages share the same
 * authoritative OAuth URLs, scopes, and client configuration.
 */
export * from '@codepilotx/core/services/oauth/constants.js'
