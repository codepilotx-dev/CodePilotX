# 安全策略

CodePilotX 会处理本地源码、命令执行、模型凭据和桌面应用更新。请负责任地披露可能影响这些边界的漏洞，不要在公开 Issue、讨论、Pull Request、日志或截图中暴露利用细节、凭据或用户数据。

## 支持范围

| 版本 | 安全更新 |
|---|---|
| 最新发布版本 | 支持 |
| `main` 分支 | 接收修复，但不视为稳定版本 |
| 更早版本 | 不支持 |

如果漏洞只存在于旧版本，请先确认最新发布版本是否仍受影响。维护者可能要求将修复前移到当前主线，而不会为所有历史版本单独发布补丁。

## 私密报告漏洞

请使用 GitHub 的 [Private Vulnerability Reporting](https://github.com/codepilotx-dev/CodePilotX/security/advisories/new) 提交报告。若该入口暂时不可用，请通过维护者 GitHub 资料页的私密联系方式告知入口故障；不要改为公开披露漏洞细节。

报告应尽量包括：

- 受影响的 CodePilotX 版本或提交；
- 漏洞类型、影响范围和现实攻击场景；
- 可复现的最小步骤或概念验证；
- 受影响的平台、配置和前置条件；
- 已知缓解措施，以及是否已在其他位置披露；
- 便于安全沟通的 GitHub 账号。

请删除 API key、访问令牌、证书、真实用户数据、会话内容和不必要的绝对路径。需要样本时请使用最小化、脱敏的数据。

## 响应流程

维护者会通过 GitHub Security Advisory 与报告者沟通，确认影响、协调修复和安排披露。响应与修复时间取决于漏洞复杂度和发布风险；在修复可用前，请不要公开利用细节。

修复通常会经过以下步骤：

1. 私密复现并确定受影响版本；
2. 在私密分支准备修复和必要回归验证；
3. 发布修复版本或明确缓解措施；
4. 在用户有合理升级时间后协调公开说明。

## 安全相关但非漏洞

- 一般加固建议可提交普通 Issue，但不得包含秘密或可直接利用的细节。
- 依赖漏洞请注明受影响包、公告编号和可用修复版本。
- 账号滥用、垃圾信息和平台级问题请使用 GitHub 自身的举报渠道。
