{
  "name": "codepilotx",
  "private": true,
  "version": "0.2.0",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/agent",
    "apps/desktop/*",
    "packages/*"
  ],
  "trustedDependencies": [
    "electron"
  ],
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "dev:agent": "bun run --cwd apps/agent dev",
    "dev:renderer": "bun run --cwd apps/desktop/renderer dev",
    "dev:desktop": "bun run scripts/dev.ts",
    "dev:electron": "bun run --cwd apps/desktop/electron dev",
    "typecheck": "bun run --cwd packages/model-schema typecheck && bun run --cwd packages/provider-plugin typecheck && bun run --cwd packages/provider-runtime typecheck && bun run --cwd packages/shared typecheck && bun run --cwd packages/agent-protocol typecheck && bun run --cwd packages/session-view typecheck && bun run --cwd apps/agent typecheck && bun run --cwd apps/desktop/renderer typecheck && bun run --cwd apps/desktop/electron typecheck",
    "models:snapshot": "bun run scripts/update-model-snapshot.ts",
    "build:renderer": "bun run --cwd apps/desktop/renderer build",
    "build:agent": "bun run --cwd apps/agent build",
    "build:desktop": "bun run --cwd apps/desktop/electron build",
    "build": "bun run scripts/build.ts",
    "package:win": "bun run build && bun run --cwd apps/desktop/electron package:win"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  },
  "patchedDependencies": {
    "@ai-sdk/google@3.0.73": "patches/@ai-sdk%2Fgoogle@3.0.73.patch",
    "@ai-sdk/xai@3.0.82": "patches/@ai-sdk%2Fxai@3.0.82.patch",
    "effect@4.0.0-beta.83": "patches/effect@4.0.0-beta.83.patch",
    "gcp-metadata@8.1.2": "patches/gcp-metadata@8.1.2.patch"
  }
}
